{% render 'page-breadcrumb' %}

<section class="about section">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          {{ page.content }}
        </div>
      </div>
    </div>
  </section>